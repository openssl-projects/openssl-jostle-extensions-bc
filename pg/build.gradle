dependencies {
    implementation project(':core')
    implementation project(':util')
    // The OpenPGP operators name JSL's Argon2, HKDF and EdDSA specs, so pg compiles against the
    // provider. No JSL type reaches pg's exported API, so the requires is not transitive.
    implementation files(rootProject.jostleProviderJar())
}
