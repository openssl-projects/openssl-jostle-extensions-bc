dependencies {
    implementation project(':core')
    implementation project(':util')
    // No provider jar: pkix:compileJava succeeds without it; the provider is reached
    // through JCA at run time, not by reference. tls is the one module that names it.
}
